Landing page for TailoredShapes — legacy system integration consultancy.

No build step. Three hand-written HTML pages and one stylesheet; edit and
commit. To preview locally:

    python3 -m http.server

The site was previously built with Tailwind. It isn't any more — `styles.css` is
the source, not an output, so don't regenerate it.
